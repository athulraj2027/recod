import type { NextFunction, Request, Response } from "express";

export default async function ErrorMiddleware(
  req: Request,
  res: Response,
  next: NextFunction,
) {
    try {
        
    } catch (error) {
        
    }
}
